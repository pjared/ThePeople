{{-- A blade full of components that I'm not sure if I'd like to use --}}

{{--    Circular Profile Image
    <img
        src="{{ $this->candidate->profile_photo_url ?? ''}}"
        alt="{{ $this->candidate->name }}"
        class="h-36 w-36 mask mask-circle border border-black"
        loading='lazy'> --}}
